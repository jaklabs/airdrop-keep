# airdrop-keep

macOS tool: auto-organize AirDrop-received files into browsable, timestamped batches in ~/AirDrop Keep so you can go back to them (no more re-sending to relocate a file).

## What it is
_(fill in: purpose, entry points, how to run)_

## Conventions
- Part of `~/claude-server` — see the root map in `../CLAUDE.md`.
- Secrets stay out of git (`.gitignore` + AWS Secrets Manager). Never commit keys/cookies/passwords.
